{
  "name": "mockserver",
  "version": "1.0.0",
  "description": "servidor de prueba destinado al uso para el desarollo y testeo de la aplicacion",
  "main": "index.js",
  "scripts": {
    "mock:server": "json-server --watch mockData.json --routes routes.json --middlewares ./middleware.js --watch login.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  }
}
